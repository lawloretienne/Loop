<?xml version="1.0" encoding="utf-8"?>
<searchable xmlns:android="http://schemas.android.com/apk/res/android"
    android:label="@string/app_name"
    android:hint="@string/search_hint"
    android:voiceSearchMode="showVoiceSearchButton|launchRecognizer"
    android:searchSuggestAuthority="com.lawloretienne.loop.providers.CustomSearchRecentSuggestionsProvider"
    android:searchSuggestSelection=" ? "
    android:includeInGlobalSearch="true"
    android:searchSettingsDescription="Videos"
    android:queryAfterZeroResults="false"
    android:searchSuggestThreshold="2"
    android:voicePromptText="This is the voice prompt"
    >
</searchable>